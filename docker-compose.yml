services:
  postgres:
    image: postgres:16-alpine
    container_name: mestra-nyx-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: mestranyx
      POSTGRES_PASSWORD: ***REMOVED***
      POSTGRES_DB: mestranyx_dev
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mestranyx"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - mestra-nyx-network

  # Redis - Cache e sessões
  redis:
    image: redis:7-alpine
    container_name: mestra-nyx-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - mestra-nyx-network

  # Adminer - Interface web para PostgreSQL
  adminer:
    image: adminer:latest
    container_name: mestra-nyx-adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - mestra-nyx-network

# Volumes persistentes
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

# Network para comunicação entre containers
networks:
  mestra-nyx-network:
    driver: bridge
